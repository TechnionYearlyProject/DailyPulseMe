ignore:
  - "BackEnd-2/src/main/java/backend/security/*.java"  #ignore the JWT that thier tests we are making manualy
  - "BackEnd-2/src/main/java/backend/controller/*.java" #ignore the controller because we cant test